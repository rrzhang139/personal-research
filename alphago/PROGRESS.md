# Progress Log

Breadcrumb trail of every implementation, experiment, and hypothesis. Each entry records the date, what was done, and the measured impact. **The next agent reads this first to understand where the project stands.**

| Date | Feature | Baseline | Result | Notes |
|------|---------|----------|--------|-------|
| 2026-02-26 | **Initial testbed** — full AlphaZero pipeline on tic-tac-toe (MCTS + PUCT, MLP dual-head net, self-play → train → arena loop, config system, CLI, `/experiment` command) | Random play | 97% win rate vs random after 25 iters (3 min CPU). 38 tests pass. | All core components: game engine, MCTS search, neural net, training pipeline, arena eval |
| 2026-02-26 | **Logging & diagnostics** — compact table output with config diff from defaults, MCTS diagnostics (policy entropy, root value, search depth), 6-panel training plots, W&B integration with grouped metrics | Verbose per-stage print statements | H(pi) drops 1.47→1.0, search depth increases 1.9→3.1 over training. Plots auto-saved. history.json for offline analysis. | Metrics: loss/{total,policy,value}, eval/{vs_random,arena}, mcts/{entropy,root_value,depth}, self_play/{outcomes,game_length} |
| 2026-02-27 | **num_simulations sweep** — experiment sweeping sims=[1,5,10,25,50,100] on tic-tac-toe | Default 25 sims | 1 sim: 61% (broken). 5 sims: 90% (biggest jump). 10-25: 95% (knee). 50-100: 99% (diminishing returns). 1-sim loss is lowest (0.53) but plays worst — soft policy targets from more sims are harder to fit but far better training signal. | `experiments/20260227_num_sims_sweep/` |
| 2026-02-27 | **Connect Four** — game engine (6x7, gravity, 4-in-a-row check, left-right mirror symmetry) + baseline training | Random play | 100% vs random in 7.8m (50 sims, 25 iters). Search depth 5.6 (vs 4.4 for ttt). Arena stays competitive throughout (harder game). 56 tests pass. | `games/connect4.py`, 18 new tests |
| 2026-02-27 | **Baselines system** — reproducible reference models per game with consistent params (50 sims, 4x128 MLP, 25 iters) for cross-game comparison. Checkpoints + history stored. | — | TTT: 95-100% vs random (3.5m). C4: 100% vs random (7.8m). | `baselines/` with README, see `ROADMAP.md` for progression plan |
| 2026-02-27 | **Othello (6x6)** — game engine with pass moves, flipping logic, 8-fold symmetry. Interface changes: `check_terminal(state, action, player)` and `get_valid_moves(state, player)` — backward-compatible defaults. 35 new tests (91 total). | Random play | 60% vs random (74% peak), 16.7m. MLP baseline struggles — spatial patterns need CNN. Only 5/25 models accepted. | `games/othello.py`, first game with pass moves, establishes MLP ceiling for spatial games |
| 2026-03-01 | **Parallel self-play** — multiprocessing pool for game-level parallelism. `training/parallel.py` with serialize/reconstruct model pattern, worker initializers. `num_workers` config + `--num-workers` CLI. Fork on Linux, spawn on macOS. `baselines/*/args.json` for reproducibility. 7 new tests (117 total). | Sequential self-play | Othello 200 sims: 4 workers gives 2.62x self-play speedup, 2.27x total. 8 workers: 2.33x total (diminishing — spawn overhead). | `training/parallel.py`, `experiments/20260301_parallel_speedup/report.md`. macOS spawn overhead ~3s/pool; Linux fork will be faster. |
| 2026-03-01 | **Virtual loss batching** — batched NN eval within MCTS. `node.py`: `apply_virtual_loss()`/`revert_virtual_loss()` (W+=1, N+=1 along path — makes nodes less attractive via -child.Q convention). `search.py`: refactored into `_run_sequential()` and `_run_batched()` with dedup. `predict_batch()` on both net wrappers. `nn_batch_size` config + `--nn-batch-size` CLI. 7 new tests (117 total). | Sequential MCTS (batch=1) | Per-game on Othello 200 sims: batch=8 → 1.53x, batch=16 → 1.79x, batch=32 → 2.14x. | `mcts/search.py`, `mcts/node.py`. VL sign: W+=1 (not -=1) because parent uses `-child.Q`. |
| 2026-03-01 | **Parallel + VL experiment** — combined game parallelism (4 workers) with virtual loss (batch=8) on Othello 200 sims. | Sequential baseline (67.8s/iter) | 4w alone: 30.3s (2.24x). 4w+VL: 26.2s (2.59x). Self-play: 39.5→11.1s (3.56x). Strategies multiply — 89% of theoretical 4.0x. Remaining gap from spawn overhead. | `experiments/20260301_parallel_virtual_loss/report.md`. Pool creation is now the bottleneck (~6.8s/iter on macOS). |
| 2026-03-01 | **CUDA+fork fix** — force CPU in worker models (monkey-patch `torch.cuda.is_available` during reconstruction), use spawn when `torch.cuda.is_initialized()`. | Fork crash on GPU machines | Workers now run on CPU regardless of main process device. Spawn used automatically on GPU machines. | `training/parallel.py`. Key learning: cloud vCPUs are ~6x slower than Mac; spawn context reimports PyTorch+CUDA (~15s/worker). Sequential + GPU VL batch is better pattern for cloud. |
| 2026-03-01 | **CNN vs MLP on Othello** — ResNet CNN (4 res blocks, 64 filters) vs MLP (4x128) on 6x6 Othello. Full baseline config (50 sims, 25 iters, 100 games, 10 epochs). Run on RTX 3080 Ti with nn_batch_size=32, sequential. | MLP baseline (60% vs random, 5/25 accepted) | CNN: 62% final (73% peak), loss 1.37, **9/25 accepted**. MLP: 64% final (73% peak), loss 1.83, 6/25 accepted. CNN loss 25% lower, value loss 39% lower, 50% more models accepted. But win rates similar — MCTS compensates for MLP's weaker priors at 50 sims. | `experiments/20260301_othello_cnn_vs_mlp/report.md`. CNN on GPU: 0.17s/game (batch=32). CNN training 70x slower on CPU but fast on GPU. Next: lower sim sweep, head-to-head. |
| 2026-02-28 | **ResNet CNN 512f, 80 iters** — scaled up ResNet to 512 filters on Othello 6x6 with extended training (80 iters). | CNN 64f (62% final, 73% peak) | Plateaued at 50-62% vs random. Low training loss but no play strength improvement. Models accepted sporadically. | `experiments/20260228_othello_cnn_vs_mlp/`. Diagnosis: ResNet preserves spatial dims (wrong arch for this task), FIFO buffer mixes stale data from weak early models. |
| 2026-03-02 | **Reference architecture port** — ported alpha-zero-general OthelloNNet (plain CNN with shrinking spatial dims + large FC layers + BN + dropout) and window buffer strategy (keep last N iterations, drop oldest). New `othellonet` network type, `buffer_strategy`/`buffer_window` config. | ResNet 512f (50-62% plateau) | Implementation only — experiment pending on GPU. | `neural_net/othello_net.py`, `experiments/20260302_othello_reference/`. 7 new tests. Two root cause fixes: (1) plain CNN vs ResNet, (2) window buffer vs FIFO deque. |
| 2026-03-04 | **Critical bugfix: winner detection in Othello** — `play_arena_game` and `play_vs_random` assumed the player who made the last move won (`return player`). In tic-tac-toe/Connect4 this is true (game ends on winning move). In Othello, game ends when neither can move — winner is by piece count, and the last mover can LOSE. Fix: check `value > 0` to determine actual winner. Same bug in 4 places: arena game, vs_random sequential, vs_random parallel worker, self_play outcome stats. Training targets were already correct (used `terminal_value` directly). | 52-76% vsRandom plateau across ALL Othello experiments | After fix: 85% vsRandom in 2 iterations with tiny 32-filter net, 25 sims. The model was learning all along but metrics couldn't measure it. | `training/arena.py`, `training/self_play.py`. This bug caused ~50% of wins to be miscounted, making arena acceptance random and vsRandom cap at ~60-75%. All previous Othello experiments need re-evaluation. |
| 2026-03-04 | **Othello OthelloNNet validated** — ran 25 iters with bugfix, OthelloNNet 512f, 25 sims, nn_batch=8, window buffer. RTX A4000. | MLP baseline 60% (broken metrics) | **98% final, 99% peak** vs random. 7/25 models accepted. 164 min. Othello is solved at this scale. | `experiments/20260302_othello_reference/`. Confirms: (1) OthelloNNet architecture works, (2) window buffer works, (3) 25 sims sufficient for 6x6 Othello with correct eval, (4) nn_batch=8 gives proper depth 3.2. |
| 2026-03-04 | **Othello baselines restructure** — split `baselines/othello/` into `mlp/` and `othellonet/` subdirs. Created `generate.py` to run both architectures and produce comparison plot. Previous MLP baseline (60% vs random) invalidated by winner-detection bugfix. | Flat single-arch baselines | Structured multi-arch baselines with generation script. Pending re-run on GPU to populate correct metrics. | `baselines/othello/generate.py`, `baselines/README.md` updated. Other games (tictactoe, connect4) unchanged — single arch each. |
| 2026-03-05 | **CNN vs MLP on Othello 6x6 (post-bugfix)** — definitive architecture comparison. Both trained 25 iters, 50 sims, 100 games/iter on RTX A4000. 40-game head-to-head after training. | Pre-bugfix: both ~60-75% vs random | **Both reach 100% vs random.** But MLP wins head-to-head **40-0**. MLP: 11/25 accepted, depth 7.1, entropy 0.42, 69m. CNN: 6/25 accepted, depth 4.8, entropy 0.61, 218m. CNN has lower loss (1.22 vs 1.75) but higher entropy — sharper value estimates but less focused search. MLP's deeper search (7.1 vs 4.8) wins decisively. | `experiments/20260304_othello_cnn_vs_mlp/`. Key insight: 6x6 Othello is simple enough that MLP + MCTS beats CNN — the spatial inductive bias doesn't help when MCTS can compensate. CNN may shine on 8x8 where MLP can't search deeply enough. Pod cost: ~$0.82 (4.8 hrs × $0.17/hr). |
